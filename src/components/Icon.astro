---
import { iconPaths, type IconName } from "@utils/icons";

interface Props {
  name: IconName;
}

const { name } = Astro.props;
---

<svg
  class="icon"
  viewBox="0 0 24 24"
  fill="currentColor"
  aria-hidden="true"
  focusable="false"><path d={iconPaths[name]}></path></svg
>
